# Merge Layers

**Action:** `mergeLayersNew`
**Target:** None (implicitly targets selected layers)

Merges the currently selected layers (or the active layer/group) into a single layer.

## JSON Structure

```json
{
  "_obj": "mergeLayersNew"
}
```

