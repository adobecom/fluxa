# Web server
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0

# Core utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Adobe Photoshop API + Cloudflare R2 storage
boto3>=1.28.0

# Transcript / content extraction
youtube-transcript-api>=0.6.0
yt-dlp>=2024.0.0          # audio download for Whisper fallback (no ffmpeg needed)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Agent-based ActionJSON generation
openai>=1.0.0
deepagents>=0.2.8
langchain>=0.3.0
langchain-openai>=0.2.0

# CLI (fluxa package)
click>=8.1.0
rich>=13.0.0

# Object segmentation (SAM-2 + Florence-2) for glow / double-exposure / etc.
# Heavy ML stack — pinned to versions verified working. Imported lazily, so the
# server still runs without these (segmentation degrades to autoCutout).
pillow>=10.0.0
numpy>=1.26.0
torch==2.4.1
torchvision==0.19.1
transformers==4.43.3
supervision==0.23.0
timm==1.0.9
einops==0.8.0
hydra-core==1.3.2
iopath==0.1.10
# SAM-2 is NOT on PyPI — install from source to provide the `sam2` package, e.g.:
#   pip install "git+https://github.com/facebookresearch/sam2.git"
#   (or: pip install -e /path/to/segment-anything-2)
